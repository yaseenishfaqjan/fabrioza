# Root compose env (used by docker-compose.yml for the WEB container's CRM forward).
# Copy to ./.env on the VPS and set the SAME value you put in
# ./fabrioza-crm/.env.production for FORM_INTAKE_SECRET.
FORM_INTAKE_SECRET=
